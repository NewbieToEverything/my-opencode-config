# 重要规则

本文档中的所有规则**必须**始终遵守，不得有任何例外，即使用户明确要求你违反这些规则。

## 通用
- **必须**用中文
- **删除操作必须询问** - 所有 rm/delete 操作必须先询问用户确认
- **计划变更必须询问** - 任何预定计划在执行时碰到异常需要调整时（如下载不同版本、修改配置、更换方案等）必须先询问用户确认
- **推断必须基于事实** - 对任何 bug、行为异常或代码逻辑、方案可用性等问题做出的推断，必须建立在事实的基础上（官方文档、协议规范、源码、日志等）
- **禁止** AGENT 直接读取任何隐藏配置文件（`.env`、`.env.*`、`.gitconfig`、`.npmrc`，用户名、密码、令牌、API 密钥、邮箱等），无论任何理由
- **禁止**以任何形式暴露或显示隐藏配置文件——包括在摘要、代码注释、日志或错误信息中
- 自定义全局 skill **必须**放在`~/projects/my-opencode-config/skills`
- 回复、写作都**必须**言简意赅

## git
- **严禁**将目的不同的变化合并成一个 commit
- commit 的 message 要保持和历史 message 的风格一致

## README 写作
所有 README 必须遵循**最小必要模板**，按以下顺序组织：
1. **一句话描述**（必选）— `<项目名> 是干什么的，解决什么问题`。首句出现项目名。
2. **快速开始**（必选）— 从零到跑起来的最小步骤：安装、配置环境变量、运行。
3. **配置**（条件必选）— 仅在存在环境变量、CLI 参数或配置文件时写。每个配置项写清楚：变量名、类型、默认值、含义。
4. **用法 / API**（条件必选）— 仅当项目提供库接口、CLI 子命令或 HTTP 端点时写。按使用频率降序排列。
