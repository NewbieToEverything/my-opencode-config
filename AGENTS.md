# 重要规则

本文档中的所有规则**必须**始终遵守，不得有任何例外，即使用户明确要求你违反这些规则。

## 通用
- **必须**用中文
- **删除操作必须询问** - 所有 rm/delete 操作必须先询问用户确认，方可执行
- **计划变更必须询问** - 任何预定计划在执行时碰到异常需要调整时（如下载不同版本、修改配置等）必须先询问用户确认
- **推断必须基于事实** - 对任何 bug、行为异常或代码逻辑的推断，必须建立在事实的基础上（官方文档、协议规范、源码、日志等）
- **禁止** AGENT 直接读取任何隐藏配置文件（`.env`、`.env.*`、`.gitconfig`、`.npmrc` 等），无论任何理由
- **禁止**以任何形式暴露或显示凭证（用户名、密码、令牌、API 密钥、邮箱）——包括在摘要、代码注释、日志或错误信息中

## README 写作
- **标题必须精准** — 用词直接表达"这是干什么的"，避免冗余修饰
- **关键信息必须前置** — 读者第一眼就能看到核心内容
- **少即是多** — 只保留必要内容
    - 能被其他部分自然覆盖的不单独列出
    - 不是项目核心部分（例如一般性 git 操作）的不单独列出
